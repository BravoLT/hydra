using System;
using DayOneConsoleAppLogic;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace DayOneConsoleAppLogicTests
{
    [TestClass]
    public class AnimalTests
    {
        [TestMethod]
        public void CanCalculateAmountOfFoodRequiredToBeFullSmallValue()
        {
            //Animal testAnimal = new Animal("testy the animal", 180f);
            //testAnimal.CalculateFoodAmount(9);

            //Assert.IsTrue(testAnimal.FoodRequiredToBeFull == 0, "the amount required is actually" + testAnimal.FoodRequiredToBeFull);


        }

        [TestMethod]
        public void CanCalculateAmountOfFoodRequiredToBeFullMediumValue()
        {
            //Animal testAnimal = new Animal("testy the animal", 180f);
            //testAnimal.CalculateFoodAmount(39);

            //Assert.IsTrue(testAnimal.FoodRequiredToBeFull == 0, "the amount required is actually" + testAnimal.FoodRequiredToBeFull);


        }

        [TestMethod]
        public void CanCalculateAmountOfFoodRequiredToBeFullLargeValue()
        {
        //    Animal testAnimal = new Animal("testy the animal", 180f);
        //    testAnimal.CalculateFoodAmount(99);

        //    Assert.IsTrue(testAnimal.FoodRequiredToBeFull == 0, "the amount required is actually" + testAnimal.FoodRequiredToBeFull);


        }
    }
}
